# Legacy v0.5.1-v0.5.2 combined upload

Status: **incomplete / not restorable**.

This directory was started by an earlier upload attempt and currently contains only:

- `source.tar.zst.b64.part00`

The remaining pieces were never committed. Do **not** treat this directory as a complete release archive.

It is intentionally preserved rather than deleted so that the repository history remains auditable.

## Version status

- v0.5.2 now has a separate, complete, byte-verified archive at `/archive/v0.5.2/`.
- v0.5.1 still requires its exact original release source or original Git bundle before it can be marked complete. It must not be reconstructed by guessing backwards from v0.5.2.

See `/VERSIONS.md` for the current repository-wide status.
