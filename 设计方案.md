# 财富自由指南灯的模型与实现

## 主线和平台变体

本地主线服务电脑上的持久使用：Python / FastAPI 提供回环 API，SQLite 保存事实，原生 HTML / CSS / JavaScript 展示、Canvas 播放反馈。无前端打包、云账户或遥测。微信变体在 variants/wechat 保留其页面、wx Storage 与平台实现。

两端当前不共享运行时代码；共享的是自由事件语义、整数金额边界、起始本金与独立资产的区别、成本校准和 FREE 定义。新功能先回答是否改善“输入真实事件 → 看见自由变化 → 复盘选择 → 更容易继续记录”，再决定各平台交互，不逐文件照搬小程序。

## 数学边界

金额先以 Decimal 验证到分，再使用整数和 Fraction 计算日边界，拒绝亚分、负数和非有限数。

~~~text
净储蓄 = 累计新增财富 - 累计生活消耗
账本日均成本 = 累计生活消耗 / 观察跨度
完整自由日 = floor(max(净储蓄, 0) / 日均成本)
           + floor(启用的独立资产 / 日均成本)
点亮格数 = min(完整自由日, 剩余人生天数)
~~~

两个桶分别取整。起始自由本金是净储蓄中的系统收入，修改它保留基准日期；它不决定真实观察起点、不计习惯记录。独立资产桶不被负净储蓄自动扣减。上手模式只使用手动成本并停用高级参数。

观察跨度默认由首笔有效真实记录到服务器本地今天；天数覆盖会重算账本成本。出生前与未来记录保留在库中但排除当前模型。没有成本不产生自由日；剩余零天不等于覆盖达成。闰日目标按目标年二月最后一天处理。

交易先持久化再产生 before / after 反馈；校准与历史修正直接更新真值。动画有时间和音效调度上限，跳过、页面隐藏与取消都会收尾；前端最终重新读取后端状态。Canvas 的缩放阶段绘制可见矢量方格，避免放大位图。

## 吸收的功能与本地实现

| 微信来源阶段 | 吸收的核心思想 | 本地落点 |
| --- | --- | --- |
| 0.3.3–0.4.x | 日格内核、缩放仪式、主题、准确整数边界、成本校准、音效与异常收尾 | model.py、grid.js；沿用本地 Canvas 并补齐边界 |
| 0.5.0–0.5.2 | 自由事件语义、上手 / 高级模式、手动成本真值、完整备份、长期账本 | 设置、引导、原子 JSON 恢复、SQLite 与分页账本 |
| 0.6.0 | 分类、标签、性质、周月年分析与同期比较 | analytics.py、消费分析、可读 CSV；SQLite 替代 wx 月桶 |
| 0.6.1 | 小数反馈、记录连续性、成就、常用入口、温和提示、上周回顾 | 记录中心及可关闭的反馈模块 |
| 0.6.2 | 清晰导航、完整指南、重播引导、模块顺序、等比字号、真正隐藏模块 | 桌面双列与窄屏单列、七档字号、设置与指南 |

上游尚未实现的“每日一张真实洞察卡”和未提供的授权字体不伪造成已完成能力。此版不增加债务会计、收益预测或双向同步。

## 数据与接口

数据库由 backend/database.py 维护版本与迁移；旧记录的主字段原样保留，新分类默认为未知。删除旧的“日期 + 类型 + 金额”唯一索引，允许同价事件并存；客户端提交标识防止一次保存因重试重复落盘。

backend/store.py 管理设置、交易、标签、完整 JSON；backend/model.py 只计算；backend/analytics.py 负责消费与习惯；backend/main.py 提供路由。具体接口方法与校验以 main.py 和 schemas.py 为准。

写操作使用 SQLite 事务；恢复检查 JSON 格式、ID、重复本金、标签和提交标识，先预览并绑定当前 revision，再备份与原子替换。拒绝新版数据库降级打开。页面内容做文本转义；CSV 中公式起始字符做中和；接口检查回环 Host 与跨站写入来源。文件位置与隐私范围以 README 为唯一入口。

## 验证与真实边界

本地 tests 覆盖精确金额与桶、观察跨度、日期边界、迁移、真实 HTTP 增删改、幂等、分类分析、备份恢复、CSRF 与 CSV。浏览器验收从空库启动，实际操作设置、交易、动画跳过与自然结束、编辑、标签、趋势、导出和恢复，检查窄屏大字号与外网请求。

本次也对原用户账本的 SQLite 副本验证：迁移前后原有交易 ID、日期、类型、金额、备注、创建时间与旧设置一致，再处理真实库。自动化不能验证用户对本金、生活成本或消费性质的判断。微信的 Node 回归与静态检查不等于微信开发者工具 / 真机验收。Windows 为当前实测平台。
