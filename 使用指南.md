# 财富自由指南灯使用指南

## 开始使用

运行方式、数据位置和备份位置以根目录 [README.md](README.md) 为准。进入页面后先阅读简短引导，也可以选择已有 JSON 备份恢复。

上手模式填写出生日期、目标年龄、日均生活成本和起始自由本金。目标年龄只是一条人生坐标，不是寿命预测；日均成本可以先用自己熟悉的月度生活开销除以 30，再按实际情况校准。本金作为一笔“系统基准收入”进入账本净储蓄，不计入记录天数。

旧本地账本保留原参数和记录，不会自动改为上手模式，也不会猜测历史分类。

## 记录真正的自由事件

“新增财富”是可用于生活的净新增资源；“生活消耗”是实际发生的个人成本。同一天、相同金额的不同消费可以分别记录。

账户互转、借款本金、投资资产内部移动、已经记过消费后的信用卡还款，不重复计为收支。退款或报销优先在“完整账本”修改原支出为最终净消耗；全额退回则删除原记录。这个应用没有扩展为复式会计、债务或投资账户管理系统。

金额精确到分。主分类只选一个；快捷标签可带入场景、默认备注与消费性质，但每次金额都由你填写。标签移除不改历史账目。性质“必要 / 可调 / 可省 / 未设定”只用于自我复盘，不改变计算，也不替你评判消费。

记录保存后，系统先完成真实写入，再播放天数变化。跨过整数日才点亮或熄灭一格，小数变化仍在轻提示中显示。可随时跳过动画；切换页面或关闭动画不会撤回已经保存的交易。

## 读懂自由天数

净储蓄是累计新增财富减去累计生活消耗。使用手动成本时，净储蓄除以日均成本，向下取整为完整自由日。高级模式的额外起始资产独立折算，不能与本金重复填写同一笔钱；负净储蓄不会直接扣除独立资产桶。

高级模式可改为“账本估计”：累计生活消耗除以首笔真实记录至今天的自然跨度。没有消费时暂不折算。天数覆盖会同步重算成本。漏记、补记较早记录和时间推移都可能改变估计，不能将所有变化理解为某一笔交易带来的财富增减。

保存设置、编辑 / 删除历史和首次建立成本，直接重算，不播放财富获得仪式。切换上手模式会停用高级参数；切回高级模式可继续使用保留的参数。

FREE 表示在当前生活成本和人生坐标下，现有自由资源覆盖了剩余区间。它不预测投资收益、通胀或未来支出，不是永久退休证明。剩余区间已结束也不会自动视作 FREE。超出区间的自由天数仍保留在统计中。

## 看见钱花到哪里

“消费分析”可查看任意周、月、年：当前周期与上一周期的对应进度比较，历史完整周期与前一完整周期比较。分类与性质统计包含“未分类 / 未设定”，避免把未知事实藏起来。

趋势可按主分类、快捷标签筛选；导出 CSV 是所选周期的可读消费记录，适合表格分析，不是完整恢复文件。“上周回顾”打开上一完整自然周，不以虚构动态卡片替代真实变化。

## 持续记录与阅读偏好

记录中心从账本计算累计天数、当前连续、最长连续、覆盖率与十二周活动格；起始本金不算签到，今天还没结束时保留截至昨天的连续链。成就、漏记提示、常用入口和周回顾都可以关闭。没有发生自由事件就不需要补一笔假账。

设置可调整两个主题、两类系统字体、七档字号和四个首页模块的顺序。关闭记录中心后整块隐藏；使用指南一直可访问，也可重播新手引导。

## 备份、恢复与迁移

在设置中导出完整 JSON，保留到可信位置。导入时先查看来源、交易量、金额与自由天数；确认后系统自动备份当前数据库，再原子替换账本、设置、标签。预览后如果账本发生变化，必须重新预览，防止覆盖新记录。

支持本地 JSON 和微信小程序 v1 / v2 JSON。微信源码 ZIP 只包含程序，不包含手机账本。当前只实现微信备份导入本地；不提供反向同步或自动云同步。异常 JSON 不会部分写入。历史中出生前或未来日期的记录仍保留，但不进入当前自由模型，页面会提示排除量。

需要从自动 SQLite 备份回退时，先关闭应用，另存当前 `data/ledger.db`，再用选中的备份替换它后启动。不要在进程运行中直接覆盖数据库文件。

## 旧账单批量导入

批量导入是可选命令行工具。CSV 不需额外解析库；XLSX / PDF 先安装 `backend/requirements-import.txt`。

~~~powershell
.venv/Scripts/python -m pip install -r backend/requirements-import.txt
.venv/Scripts/python backend/import_bills.py 'imports/支付宝账单.csv'
# 核对预览后再写入；可用 --since 2026-01-01 明确过滤日期
.venv/Scripts/python backend/import_bills.py 'imports/支付宝账单.csv' --apply
~~~

默认只预览，不写账本。不传路径时扫描 imports。支付宝 CSV 文件名需含“支付宝”或“alipay”；其他 CSV 按银行格式读取。解析器保留原有微信 XLSX、工行 PDF / CSV 和支付宝 CSV 格式支持，不承诺适配所有银行或新模板。PDF 是表格启发式提取，必须核对原账单金额与条目；本次未用私人原始票据做格式验收。

重复导入同一原文件不会重复记账，同文件中的两笔同价事件会保留。不同导出文件可能含相同交易：目前不会跨文件猜测合并；先排除重叠日期、渠道、互转和退款。原有账本中已导入但缺少文件指纹的历史记录，也需人工避免重复导入。

## 演示数据

仅在单独的测试账本体验：

~~~powershell
.venv/Scripts/python backend/seed_demo.py --db tmp/demo.db
$env:FORTUNE_DB_PATH = (Join-Path $PWD 'tmp/demo.db')
python run.py --port 8767
~~~

已有交易时默认拒绝重置；只有显式 `--confirm-reset` 才备份后替换。日常账本无需执行演示命令。结束演示后关闭该终端，正常启动即可回到默认账本。
