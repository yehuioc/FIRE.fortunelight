# FIRE.fortunelight

财富自由指南灯（WeChat Mini Program）版本历史仓库。

当前以“可验证、可恢复”为归档标准，不把不完整传输标记为已完成。

## 当前状态

- v0.2.0–v0.5.0：完整历史源码归档，见 `archive/all-versions/`
- v0.5.2：完整、逐块校验并可恢复，见 `archive/v0.5.2/`
- **v0.6.2：当前最新已发布归档，15/15 分块完成字节级校验，见 `archive/v0.6.2/`**
- v0.5.1：保留为未完成历史，不用后续版本反推伪造
- v0.6.0 / v0.6.1：存在于本地开发 Git 历史，但没有在本仓库单独发布源码快照

完整状态、恢复命令和校验值见 `VERSIONS.md` 与各版本目录下的 `README.md`。

> `main` 保存版本归档与恢复资料，并不是把最新小程序源码直接展开在仓库根目录。
