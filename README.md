# FIRE.fortunelight

财富自由指南灯（WeChat Mini Program）版本历史仓库。

当前以“可验证、可恢复”为归档标准，不把不完整传输标记为已完成。

## 当前状态

- v0.2.0–v0.5.0：完整历史源码归档，见 `archive/all-versions/`
- v0.5.2：完整、逐块校验并可恢复，见 `archive/v0.5.2/`
- v0.5.1：等待 exact source；旧 `archive/v0.5.1-v0.5.2/` 只有一个残留分块，明确标记为 incomplete

完整状态与校验值见 `VERSIONS.md`。

> `main` 当前保存版本归档与恢复资料，并不是把最新小程序源码直接展开在仓库根目录。
